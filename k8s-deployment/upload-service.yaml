apiVersion: v1
kind: Service
metadata:
  name: thumby-upload-service
spec:
  ports:
  - port: 8080
    protocol: TCP
    name: http
  selector:
    app: thumby-upload
  type: NodePort

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: thumby-upload
spec:
  selector:
    matchLabels:
      app: thumby-upload
  replicas: 3
  template:
    metadata:
      labels:
        app: thumby-upload
    spec:
      containers:
      - name: thumby-upload-app
        image: gcr.io/carbide-parser-205909/upload-service:1
        ports:
        - containerPort: 8080
        command: ["java"]
        args:
        - -Djava.security.egd=file:/dev/./urandom
        - -jar
        - /upload-service.jar
        - --gcn.bucket-name=thumbnail-service
        - --thumbnail-service.port=8888
        - --thumbnail-service.hostname=thumby-thumb-generator-service.default
